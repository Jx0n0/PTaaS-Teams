class ScopedPermissionMixin:
    """
    Reserved mixin for future Customer/Project scope-control expansion.
    Current phase keeps role-only baseline and object scoping in ScopeService.
    """

    scope_enabled = True
